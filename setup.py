#!/usr/bin/env python3

from setuptools import find_packages, setup

setup(
    name='src',
    version='0.1',
    description="Hate Speech Detection with Transformers.",
    author='Jessica Silva',
    packages=find_packages(),
)
